"""Factories for persistent domain objects.

Empty because there are no ORM models yet (no Postgres schema beyond
the LangGraph checkpointer tables). Once account/strategy/approval/
audit models exist, add factory_boy (or equivalent) factories here —
one module per model, mirroring app's future models package — instead
of hand-rolled fixtures, per CLAUDE_QA_INSTRUCTIONS.
"""
