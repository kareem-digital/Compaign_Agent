/**
 * Remote entry point. When this app is exposed as a Module Federation remote,
 * this module is what the host consumes.
 */
export { VowAgentWidget } from "@/widget/VowAgentWidget";
export type { VowAgentWidgetProps } from "@/widget/VowAgentWidget";
